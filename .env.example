# --- Application Secrets ---
NODE_ENV=development
PORT=3001

# --- Database ---
# Since we are using Docker networking, the host is 'mongodb', not localhost
MONGO_URI=mongodb://mongodb:27017/mental_health_db
REDIS_HOST=redis
REDIS_PORT=6379

# --- Authentication ---
# Generate a strong random string for production: openssl rand -base64 32
JWT_SECRET=change_this_to_a_super_secret_string
JWT_EXPIRE=30d

# --- External APIs ---
GEMINI_API_KEY=your_google_gemini_api_key_here
TWILIO_SID=your_twilio_sid_here
TWILIO_AUTH_TOKEN=your_twilio_auth_token_here
TWILIO_PHONE_NUMBER=your_twilio_phone_here

# --- ML Service Config ---
# Allowed origins for CORS (comma separated)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001